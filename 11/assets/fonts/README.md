# Fonts

This template intentionally uses local system font stacks so it works offline and does not make font-provider requests. Add self-hosted font files here and define their `@font-face` rules in `assets/css/style.css` if custom type is required.
